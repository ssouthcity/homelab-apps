apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: sonarr-anime
spec:
  interval: 1h
  serviceAccountName: flux
  url: oci://ghcr.io/ssouthcity/charts/sonarr
  ref:
    semver: ~0
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: extract
  verify:
    provider: cosign
    matchOIDCIdentity:
      - issuer: ^https://token\.actions\.githubusercontent\.com$
        subject: ^https://github\.com/ssouthcity/charts/\.github/workflows/publish-chart\.yaml@refs/heads/main$
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sonarr-anime
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: sonarr-anime
  values:
    envFrom:
      - configMapRef:
          name: sonarr-anime
      - secretRef:
          name: sonarr-anime-auth
    cnpg:
      enabled: true
    httpRoute:
      enabled: true
      annotations:
        gethomepage.dev/enabled: "true"
        gethomepage.dev/name: Sonarr Anime
        gethomepage.dev/description: PVR for Usenet and BitTorrent
        gethomepage.dev/group: Media
        gethomepage.dev/icon: sonarr.png
        gethomepage.dev/pod-selector: "app.kubernetes.io/instance=sonarr-anime"
      parentRefs:
        - name: http
          namespace: kgateway
      hostnames:
        - sonarr-anime.${CLUSTER_DOMAIN}
    volumes:
      - name: media
        persistentVolumeClaim:
          claimName: media
    volumeMounts:
      - name: media
        mountPath: /data/torrents/anime
        subPath: torrents/anime
      - name: media
        mountPath: /data/media/anime
        subPath: media/anime
